# Custom partition table for ESP32 with 16MB flash (used via PartitionScheme=custom).
#
# Layout (matches the core's tools/partitions/default_16MB.csv):
#   - two OTA app slots of 6.25MB each -> plenty of headroom for growth
#   - a 3.375MB SPIFFS partition, mounted by LittleFS for /config.txt (+ future logs)
#   - coredump kept for crash analysis
#
# NOTE: changing the flash map requires a ONE-TIME USB re-flash of the whole
# image (bootloader + partitions + app). Over-the-air update from the previous
# 4MB layout will NOT move partitions.
#
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x640000,
app1,     app,  ota_1,   0x650000, 0x640000,
spiffs,   data, spiffs,  0xC90000, 0x360000,
coredump, data, coredump,0xFF0000, 0x10000,
